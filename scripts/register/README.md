# Register Scripts

Node registration scripts for Localchimera.

## Files

- `register-node.ts` — Register a new inference/mining node with the network

## Usage

```bash
npx tsx scripts/register/register-node.ts
```

Set the required environment variables (RPC URL, node key, etc.) before running.
